services:
  portfolio:
    build:
      context: .
    ports:
      - "4173:4173"
    env_file:
      - .env
    environment:
      PORT: "4173"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
